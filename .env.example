# ═══════════════════════════════════════════════════════════════
#  ENVIRONMENT VARIABLES
#
#  HOW TO USE:
#  1. Copy this file:  cp .env.example .env
#  2. Fill in real values in .env
#  3. Never commit .env to git (it's in .gitignore)
#
#  WHY THIS PATTERN?
#  .env.example is committed → your team knows what vars are needed.
#  .env is gitignored        → your secrets stay off GitHub.
# ═══════════════════════════════════════════════════════════════

# Anthropic API key for the AI Recommender Service
# Get yours at: https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# JWT signing secret — must be at least 256 bits (32+ chars)
# Generate one: openssl rand -hex 32
JWT_SECRET=replace_with_a_long_random_string_at_least_32_characters
